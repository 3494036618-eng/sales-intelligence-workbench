# 变更记录

本文件记录各发行版本的公开变化。当前版本为 `0.10.0` 自托管开源版。

## 0.10.0 - 2026-08-03

- 将公开身份流程统一为单工作区、单管理员：首次使用只设置用户名和密码，后续直接登录。
- 移除浏览器邮箱字段、邮件确认、公开注册、邮件找回密码及成员/角色相关产品表述。
- 修复短期访问令牌过期后无法使用长期会话自动恢复的问题；同一浏览器默认保持登录一年。
- 保留现有账号和数据的兼容读取；底层归属记录继续用于鉴权与数据隔离，不作为多人协作功能开放。
- Job 因 Worker 中断而失败或取消时，自动结束关联的 Provider Run 和运行中步骤，并修复历史悬挂记录。
- 档案生成改为主体锚定和事实级引用门禁：专业来源必须能以法定名称或统一社会信用代码确认目标主体，引用数量不再作为生成成功条件。
- 拒绝验证码、人机校验和访问拦截页进入证据包。
- 将档案生成重构为有界单 Agent：通过 Agent Plan Responses API 强制调用严格函数提交固定六章节档案，服务端独立执行证据与展示质量门禁。
- 移除档案文本 JSON 修补和模型二次自由成稿；Agent 首次提交完整六章节规划，必要时最多定点修订一次，服务端确定性组装六章正文和引用，连续不合格不保存新档案。
- 增加确定性上下文预算：最多选择 10 条高信号证据，去除模型不需要的诊断字段，并把函数输出固定为六章各一个完整段落，避免条目膨胀和长时间重复修订。
- 增加最终公开视图门禁：清洗后仍须保留被正文实际使用的法定主体锚点和逐段有效引用；摘要只从最终可见且有引用的章节重建。
- 保存前只校验六章节实际引用的来源，未被正文引用的证据不能替代主体锚点或事实支撑；不合格历史档案在列表和按 ID 详情接口中均不可见。
- 对“品牌（中国）投资有限公司”补充受限品牌别名检索，品牌动态标记为 `alias_scoped`，不得冒充法定主体事实。
- 权威站点补搜由“固定公开来源数量不足”改为“尚无可用近期业务事件”触发，并设置有限查询次数和无新增即停止的资源边界。
- 档案检索增加主体、经营、近期事件、风险、招采项目和来源独立性的覆盖评估，仅对缺失主题执行最多 4 次补充检索。
- 档案规划不设置整份报告最低来源数量，每个事实只绑定最少且直接的证据；可靠来源较少时保持短报告，不用弱来源凑数。
- 严格函数模型调用对网络、限流、超时和上游临时故障默认有界重试一次；鉴权、配置和内容门禁错误不盲目重试。
- 将 Provider 返回的 `Internal Error` 识别为上游临时故障并纳入有界重试；档案 Agent 最多执行两次完整六章节规划或定点修订，同时保留日期、数值、机构和事件关系门禁。
- 档案采集改为专业数据集最多 2 路、联网搜索最多 3 路有界并发；默认专业查询超时调整为 45 秒。
- 档案任务逐项持久化已完成查询和证据包；可重试故障按退避重新入队并只继续未完成查询，避免 Worker 中断后重复已经成功的付费调用。
- 前端进度改用紧凑阶段文字和完成计数，不显示容易长时间停滞的百分比，也不暴露 Worker、检查点或 Provider 错误栈。
- 事实门禁不再把已核验法定名称中的“投资”等词误判为新事件；每章固定一个完整段落，避免额外可选条目放大输出或让无关内容拖累整份档案。
- 规划门禁改为在模型已选择的有界来源摘要上校验日期、数值、机构与事件，同时继续逐字校验短摘录的连续性；短摘录过短不再误伤已有完整来源支撑的事实。
- 模型在来源只有月日时擅自补全年份，经过一次定点修订仍未纠正的，服务端只删除不受支持的年份精度并保留已核验月日，不补造日期或替换来源。
- 经过一次定点修订后，连续但过长的展示摘录由服务端只截短到 120 字；同一条内容仍有有效直接证据时才删除抄写不连续的附加摘录，唯一证据无效时继续失败。
- 建议行动在有界修订后仍包含来源未支持的英文缩写时，只把该行动对象降为“相关业务”；外部事实中的日期、数值、机构和英文实体不适用该降级。
- 机构事实门禁的修订反馈会指出具体的有限机构名称，避免模型在不知道错误对象时重复盲改；该诊断只保存在后端任务记录中，前端继续显示用户友好的失败提示。
- 证据变化但最终正文及实际引用的报告指纹未变化时保留原版本，避免生成内容相同的连续档案版本。
- 将档案来源区改为专业数据集（DataPro）和联网搜索两组：专业数据集按需展开逐字段明细供核验，联网搜索只展示网页标题、来源站点、发布时间和原文链接，不再向普通用户堆叠搜索长摘要或内部质量标签。

## 0.9.2 - 2026-07-29

- 将公开身份流程统一为单工作区、单管理员：首次使用只设置用户名和密码，后续直接登录。
- 移除浏览器邮箱字段、邮件确认、公开注册、邮件找回密码及成员/角色相关产品表述。
- 新增本机交互式密码重置命令；密码只通过标准输入传递，不进入命令参数或日志。
- 保留现有账号和数据的兼容读取；底层归属记录继续用于鉴权与数据隔离，不作为多人协作功能开放。

## 0.9.1 - 2026-07-29

- 统一使用 Agent Plan 控制台正式名称：专业数据集（DataPro）、豆包搜索（联网搜索）、Agent 记忆（OpenViking）和 AI Native 应用开发底座（Supabase）。
- 将远程初始化入口固定到独立仓库的 `v0.9.1` tag，避免引用尚未发布的仓库路径。
- 公开应用统一为一种真实数据运行方式，移除开发/录屏运行参数、虚构企业、固定档案和 Provider 静态兜底。
- 移除开发路线图、验收流水、旧 Skill 兼容入口、环境特定说明和未加载的旧前端服务层，只保留用户使用与二次开发所需内容。
- 新增公开发布检查，覆盖私密文件、本机路径、内部材料、相对文档链接、版本一致性、正式产品名称与 Skill 分发包边界。

## 0.9.0 - 2026-07-23

### 新增

- 移除成员管理界面及公开成员管理 API；当前发行版按单工作区、单负责人使用，内部工作区归属记录仅用于登录鉴权和数据隔离。
- Skill 从“已有应用运维手册”升级为按 Cookbook 执行的 Builder：先确认销售场景，再安装标准前后端、配置真实资源、导入资料并验收业务闭环。
- 新增 `setup.mjs` 阶段检测，以及首批资料导入和真实业务验收的本机脱敏回执。
- Supabase Auth、工作区归属与负责人访问边界。
- DataPro 企业识别、专业数据与公开搜索取证、带引用档案和资料问答。
- 飞书 CLI 增量资料导入、OpenViking 企业级目录隔离及长期记忆检索。
- Supabase 持久化异步任务队列、独立 Worker、进度恢复、重试和安全取消。
- 工作区级付费任务并发与每日次数保护。
- Skill 安装、配置、诊断、迁移、备份、恢复、升级和卸载脚本。
- 仅负责人可调用的工作区业务数据导出接口与私密文件导出脚本。
- 关键业务写操作、Provider 探测和工作区导出的脱敏审计，以及管理员只读查询接口。
- 发布密钥扫描和一键离线发布验收。

### 安全

- 根目录加入项目许可证文本。
- 正式运行路径禁止演示数据和 Provider 静态兜底。
- Service Role、Agent Plan Key 和控制面凭证只允许保存在服务端私密配置。
- 业务接口增加认证授权、请求体上限、来源校验和安全响应头。
- `verify-real-chain.mjs --help` 只显示用法，不触发真实 Provider 请求或产生 AFP/Token。

### 已知限制

- 当前版本仅支持单工作区、单人使用，以及本机或受控内网自托管。
- 公网部署仍需 HTTPS 反向代理、Secure Cookie 和精确来源白名单。
- 版本化迁移目标更新至 `202607280002`：问答正文仅由 OpenViking 保存和检索，Supabase 只保留业务结构与 OpenViking 会话引用；旧问答表以只读归档方式保留，项目迁移元数据表启用 RLS 并仅允许后端 `service_role` 访问。
