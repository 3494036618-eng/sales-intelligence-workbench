# 销售智能工作台自托管 Beta

这是从内部开发仓库中整理出的 `0.9.0` Beta，仅支持单工作区、单人使用，以及本机或受控内网自托管。发布包不包含本地密钥、业务备份、内部测试材料和旧演示 Skill。

## 从哪里开始

1. 阅读 `README.md`，了解架构、安装和真实 Provider 配置。
2. 使用 `skills/sales-intelligence-workbench/` 作为 Codex Skill 安装入口。
3. 后端源码位于 `backend/`，前端源码位于 `frontend/`。
4. Supabase 迁移位于 `supabase/migrations/`。
5. API 与数据库说明位于 `docs/`。

## 目录结构

- `.github/`：持续集成检查。
- `backend/`：真实 API、Provider、持久化、任务与测试。
- `frontend/`：销售工作台正式前端。
- `skills/sales-intelligence-workbench/`：可安装的 Codex Skill 及其自包含应用资产。
- `supabase/`：数据库迁移。
- `docs/`：公开技术文档。
- `README.md`：项目使用说明。
- `SECURITY.md`：安全与漏洞反馈说明。
- `CONTRIBUTING.md`：贡献说明。
- `LICENSE`：Apache License 2.0；正式公开前仍需代码所有方完成开源授权确认。

## 发布前提醒

- 不要提交任何 `.env.local` 或本机配置目录。
- 根目录采用 Apache License 2.0；正式公开前仍需代码所有方确认公司代码产权和开源授权。
- 发布前应执行 README 中的测试、自检和真实 Provider 检查。
